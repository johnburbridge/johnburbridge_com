{
  "env": {
    "browser": true,
    "es2021": true,
    "node": true // Added node env for config files
  },
  "extends": [
    "eslint:recommended",
    "prettier" // Add prettier LAST
  ],
  "parserOptions": {
    "ecmaVersion": 12,
    "sourceType": "module"
  },
  "rules": {
    // Keep your specific rules if needed, but Prettier handles formatting
    // Example: You might remove indent, quotes, semi rules if Prettier handles them
  }
}
